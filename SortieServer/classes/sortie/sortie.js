module.exports = class Sortie {
    constructor(squad)
    {
        this.squad = squad; // Name of the squadron (Example: "14th ACCS")
        this.scheduled = scheduled; // Boolean to represented rather sortie has been scheduled or not
    }
}